#include <iostream>

using namespace std;;

int main() {
	
	int x = 1;
	int cot = 0;


	while (x* x + y1 * y1 <=100) {
		double a = 3.0 * -1 * x;
		double y1 = 2.0 / a;
		double y2 = 1.0 / a;
		int n = x * x + y1 * y1;

		if (y2 > y1) {

			cot++;


		}


	}
	cout << cot << endl;


}